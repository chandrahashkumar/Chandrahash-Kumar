// 9. Use a for loop to print the first 10 natural numbers.

#include<stdio.h>
int main(){
    int num = 10;
    for(int count= 1;count<=num;count++){
        printf("%d\n",count);
    }
    return 0;
}